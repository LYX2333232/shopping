import { defineStore } from 'pinia'
import { ref } from 'vue'

export const CartStore = defineStore('cp-cart', () => {
    const cart = ref(0)

    const getCartNumber = () => {

    }

    return {
    }
})